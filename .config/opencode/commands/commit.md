---
description: Draft a conventional commit message from staged changes
agent: build
---
Here is the current git status and staged diff:

!`git status --short`

!`git diff --staged`

Write a concise, conventional-commit-style commit message (type(scope): summary, blank line, body if needed) for these staged changes. Do not run `git commit` yourself — just propose the message and wait for confirmation, per the global commit-checkpoint rules.
